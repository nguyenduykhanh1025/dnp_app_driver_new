const PREFIX = ctx + "logistic/send-cont-full"

console.log(containerNo, sztp);
$('#containerNo').val(containerNo);
$('#sztp').val(sztp);

function confirm() {
    $.modal.close();
}

function closeForm() {
    $.modal.close();
}